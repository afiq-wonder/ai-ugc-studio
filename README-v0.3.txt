AI UGC Studio PWA v0.3 — iPad Performance Build

REPLACE THESE FILES IN YOUR GITHUB REPOSITORY:
- index.html
- manifest.webmanifest
- sw.js

Keep these existing root files:
- icon.svg
- icon-192.png
- icon-512.png

After committing:
1. Wait 30–90 seconds for GitHub Pages.
2. Open the site in Safari.
3. Refresh once.
4. If the old version remains, close the Safari tab and reopen the site.

Changes:
- Resizes and compresses image previews locally.
- Removes overlay text after image upload.
- Adds progress feedback.
- Collapses long prompt cards.
- Fixes saved fallback product name.
- Uses the existing root icon paths.
- Updates service-worker cache to v0.3.
